---
phase: 01-supported-workflow-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_supported_workflow_contract.py
  - src/vepyr/_workflow_contract.py
autonomous: true
requirements:
  - SCOPE-01
  - SCOPE-02
must_haves:
  truths:
    - "The repository has one executable supported-workflow source of truth for homo_sapiens, GRCh38, release 115, and vep cache flavor."
    - "Phase 1 validation paths are covered by a dedicated pytest file instead of being inferred from existing happy-path tests."
  artifacts:
    - path: "src/vepyr/_workflow_contract.py"
      provides: "Supported workflow constant, manifest filename, and validation/helper interfaces."
      contains: "SUPPORTED_WORKFLOW"
    - path: "tests/test_supported_workflow_contract.py"
      provides: "Focused tests for supported tuple, manifest expectations, and error-path coverage."
      contains: "test_supported_workflow_constant_is_release_115_grch38_vep"
  key_links:
    - from: "tests/test_supported_workflow_contract.py"
      to: "src/vepyr/_workflow_contract.py"
      via: "imports and direct assertions on the supported contract constants/helpers"
      pattern: "from vepyr\\._workflow_contract import"
---

<objective>
Establish the executable supported-workflow contract and the dedicated Phase 1 test harness before runtime wiring starts.

Purpose: Freeze the exact v1 tuple and the test surface that later plans will implement against.
Output: A new internal workflow-contract module plus a focused pytest file for supported-scope behavior.
</objective>

<execution_context>
@/Users/mwiewior/research/git/vepyr/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mwiewior/research/git/vepyr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-supported-workflow-contract/01-CONTEXT.md
@.planning/phases/01-supported-workflow-contract/01-RESEARCH.md
@.planning/phases/01-supported-workflow-contract/01-VALIDATION.md
@src/vepyr/__init__.py
@tests/test_annotate.py
@tests/test_golden.py

<interfaces>
From src/vepyr/__init__.py:
```python
def build_cache(
    release: int,
    cache_dir: str,
    *,
    species: str = "homo_sapiens",
    assembly: str = "GRCh38",
    method: str = "vep",
    partitions: int = 8,
    memory_limit_gb: int = 32,
    local_cache: str | None = None,
) -> list[tuple[str, int]]:
```

```python
def annotate(
    vcf: str,
    cache_dir: str,
    *,
    everything: bool = False,
    hgvs: bool = False,
    reference_fasta: str | None = None,
    merged: bool = False,
    failed: int = 0,
    cache_size_mb: int = 1024,
    skip_csq: bool = True,
) -> "pl.LazyFrame":
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the dedicated Phase 1 contract test file</name>
  <files>tests/test_supported_workflow_contract.py</files>
  <read_first>
    tests/test_supported_workflow_contract.py
    tests/test_annotate.py
    tests/test_golden.py
    src/vepyr/__init__.py
    .planning/phases/01-supported-workflow-contract/01-CONTEXT.md
    .planning/phases/01-supported-workflow-contract/01-VALIDATION.md
  </read_first>
  <action>Create `tests/test_supported_workflow_contract.py` as a new pytest module for Phase 1. Add a temp-dir helper named `write_workflow_manifest(cache_dir, *, species="homo_sapiens", assembly="GRCh38", release=115, cache_flavor="vep", reference_name="ensembl_grch38_primary_assembly", contig_alias_style="ensembl_no_chr", derived_fixture=False)` that writes `workflow.json` into the provided cache directory. Add focused tests named `test_supported_workflow_constant_is_release_115_grch38_vep`, `test_build_cache_rejects_release_species_assembly_or_method_outside_supported_scope`, `test_annotate_requires_workflow_manifest_before_engine_start`, `test_annotate_rejects_manifest_release_mismatch`, and `test_annotate_requires_reference_fasta_when_everything_or_hgvs_is_enabled`. Use `monkeypatch` on `vepyr._convert_entity` and `vepyr._create_annotator` so the tests stay local and never download Ensembl data or call into the Rust engine for Phase 1 validation paths.</action>
  <acceptance_criteria>
    `tests/test_supported_workflow_contract.py` exists.
    `rg -n "write_workflow_manifest|test_supported_workflow_constant_is_release_115_grch38_vep|test_build_cache_rejects_release_species_assembly_or_method_outside_supported_scope|test_annotate_requires_workflow_manifest_before_engine_start" tests/test_supported_workflow_contract.py` returns matches.
    The file contains the literal supported tuple values `homo_sapiens`, `GRCh38`, `115`, and `vep`.
  </acceptance_criteria>
  <verify>
    <automated>python -m compileall tests/test_supported_workflow_contract.py && rg -n "write_workflow_manifest|test_supported_workflow_constant_is_release_115_grch38_vep|test_build_cache_rejects_release_species_assembly_or_method_outside_supported_scope|test_annotate_requires_workflow_manifest_before_engine_start" tests/test_supported_workflow_contract.py</automated>
  </verify>
  <done>The Phase 1 validation file exists with concrete tests for the supported tuple, manifest presence, and Python-side error paths required by SCOPE-01 and SCOPE-02.</done>
</task>

<task type="auto">
  <name>Task 2: Define the internal workflow-contract module and interfaces</name>
  <files>src/vepyr/_workflow_contract.py</files>
  <read_first>
    src/vepyr/_workflow_contract.py
    src/vepyr/__init__.py
    tests/test_supported_workflow_contract.py
    .planning/phases/01-supported-workflow-contract/01-CONTEXT.md
    .planning/phases/01-supported-workflow-contract/01-RESEARCH.md
  </read_first>
  <action>Create `src/vepyr/_workflow_contract.py` with one immutable dataclass named `SupportedWorkflowContract`, one module constant named `SUPPORTED_WORKFLOW`, and one manifest filename constant named `CACHE_MANIFEST_FILENAME = "workflow.json"`. Set the contract values exactly to `species="homo_sapiens"`, `assembly="GRCh38"`, `release=115`, `cache_flavor="vep"`, `reference_name="ensembl_grch38_primary_assembly"`, `contig_alias_style="ensembl_no_chr"`, and `fixture_source="Homo_sapiens.GRCh38.dna.primary_assembly.fa"`. Add stubbed helper/function signatures for manifest and validation work that later plans will implement against: `build_supported_workflow_dict()`, `build_cache_manifest(cache_dir: str, *, derived_fixture: bool = False)`, `load_cache_manifest(cache_dir: str)`, `validate_build_cache_request(release: int, species: str, assembly: str, method: str)`, `validate_annotation_request(cache_dir: str, *, reference_fasta: str | None, everything: bool, hgvs: bool, merged: bool)`, and `validate_reference_fasta(reference_fasta: str, manifest: dict)`. Keep this module internal; do not add a public override path or support any cache flavor other than `vep` per D-02 and D-04.</action>
  <acceptance_criteria>
    `src/vepyr/_workflow_contract.py` exists.
    `rg -n "class SupportedWorkflowContract|SUPPORTED_WORKFLOW|CACHE_MANIFEST_FILENAME = \"workflow.json\"|release=115|species=\"homo_sapiens\"|assembly=\"GRCh38\"|cache_flavor=\"vep\"" src/vepyr/_workflow_contract.py` returns matches.
    The module defines the six helper function names listed in the action text.
  </acceptance_criteria>
  <verify>
    <automated>python -m compileall src/vepyr/_workflow_contract.py && rg -n "SupportedWorkflowContract|SUPPORTED_WORKFLOW|CACHE_MANIFEST_FILENAME = \"workflow.json\"|def build_supported_workflow_dict|def build_cache_manifest|def load_cache_manifest|def validate_build_cache_request|def validate_annotation_request|def validate_reference_fasta" src/vepyr/_workflow_contract.py</automated>
  </verify>
  <done>The codebase has one internal file that encodes the exact supported tuple and the manifest/validation interface future plans will implement.</done>
</task>

</tasks>

<verification>
Run `python -m compileall src/vepyr/_workflow_contract.py tests/test_supported_workflow_contract.py`.
Run `uv run pytest tests/test_supported_workflow_contract.py -q -x` after later plans wire the helpers into the Python API.
</verification>

<success_criteria>
The repo contains a dedicated test file and one contract module that both hard-code the same supported tuple: `homo_sapiens`, `GRCh38`, release `115`, cache flavor `vep`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-supported-workflow-contract/01-supported-workflow-contract-01-SUMMARY.md`
</output>
