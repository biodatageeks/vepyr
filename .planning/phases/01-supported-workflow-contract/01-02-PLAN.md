---
phase: 01-supported-workflow-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/vepyr/_workflow_contract.py
  - src/vepyr/__init__.py
  - tests/test_supported_workflow_contract.py
autonomous: true
requirements:
  - SCOPE-02
  - API-02
must_haves:
  truths:
    - "Unsupported build_cache and annotate inputs fail in Python with explicit supported-workflow errors before Rust/DataFusion execution."
    - "Missing FASTA, missing manifest, and unsupported merged/refseq workflow combinations produce actionable messages that name the supported tuple."
  artifacts:
    - path: "src/vepyr/_workflow_contract.py"
      provides: "Implemented validation and manifest-loading helpers."
      contains: "validate_annotation_request"
    - path: "src/vepyr/__init__.py"
      provides: "build_cache() and annotate() preflight validation using the workflow-contract helpers."
      contains: "validate_build_cache_request"
    - path: "tests/test_supported_workflow_contract.py"
      provides: "Assertions on exact Python-side validation messages."
      contains: "match="
  key_links:
    - from: "src/vepyr/__init__.py"
      to: "src/vepyr/_workflow_contract.py"
      via: "build_cache() and annotate() helper calls before _convert_entity/_create_annotator"
      pattern: "validate_(build_cache_request|annotation_request)"
    - from: "src/vepyr/__init__.py"
      to: "vepyr._core.create_annotator"
      via: "preflight validation happens before _create_annotator"
      pattern: "_create_annotator"
---

<objective>
Implement strict Python-side validation for unsupported workflow combinations and missing prerequisites.

Purpose: Meet the phase requirement that users get clear, actionable errors before engine startup.
Output: Preflight validation wired into `build_cache()` and `annotate()`, with tests asserting exact error paths.
</objective>

<execution_context>
@/Users/mwiewior/research/git/vepyr/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mwiewior/research/git/vepyr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-supported-workflow-contract/01-CONTEXT.md
@.planning/phases/01-supported-workflow-contract/01-RESEARCH.md
@.planning/phases/01-supported-workflow-contract/01-VALIDATION.md
@.planning/phases/01-supported-workflow-contract/01-01-PLAN.md
@src/vepyr/_workflow_contract.py
@src/vepyr/__init__.py
@tests/test_supported_workflow_contract.py

<interfaces>
From src/vepyr/_workflow_contract.py after Plan 01:
```python
CACHE_MANIFEST_FILENAME = "workflow.json"
SUPPORTED_WORKFLOW = SupportedWorkflowContract(...)

def validate_build_cache_request(release: int, species: str, assembly: str, method: str) -> None: ...
def validate_annotation_request(cache_dir: str, *, reference_fasta: str | None, everything: bool, hgvs: bool, merged: bool) -> dict: ...
def validate_reference_fasta(reference_fasta: str, manifest: dict) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement supported-workflow validation helpers in the contract module</name>
  <files>src/vepyr/_workflow_contract.py</files>
  <read_first>
    src/vepyr/_workflow_contract.py
    src/vepyr/__init__.py
    tests/test_supported_workflow_contract.py
    .planning/phases/01-supported-workflow-contract/01-CONTEXT.md
    .planning/phases/01-supported-workflow-contract/01-RESEARCH.md
  </read_first>
  <action>Implement the helper functions declared in Plan 01. `validate_build_cache_request()` must reject any tuple outside `release=115`, `species="homo_sapiens"`, `assembly="GRCh38"`, and `method="vep"` with one `ValueError` that includes the rejected values and the supported tuple. `load_cache_manifest()` must read `<cache_dir>/workflow.json` and raise `FileNotFoundError` with the literal missing path when the manifest is absent. `validate_annotation_request()` must enforce these exact rules before engine startup: `reference_fasta` is required when `everything=True` or `hgvs=True`; `merged=True` is unsupported; manifest `release`, `species`, `assembly`, and `cache_flavor` must equal `115`, `homo_sapiens`, `GRCh38`, and `vep`; related manifest mismatches should be aggregated into one `ValueError` message that lists every mismatched key. `validate_reference_fasta()` must raise `FileNotFoundError` when the FASTA path is missing, must reject a header line that does not start with `>1` or `>chr1`, and must reject contig-style mismatches between manifest `contig_alias_style` and the FASTA header.</action>
  <acceptance_criteria>
    `rg -n "def validate_build_cache_request|def load_cache_manifest|def validate_annotation_request|def validate_reference_fasta" src/vepyr/_workflow_contract.py` returns matches.
    The file contains the literal strings `release=115`, `species=\"homo_sapiens\"`, `assembly=\"GRCh38\"`, `cache_flavor=\"vep\"`, and `workflow.json`.
    The module raises `ValueError` or `FileNotFoundError` for all unsupported-scope cases described in the action text.
  </acceptance_criteria>
  <verify>
    <automated>python -m compileall src/vepyr/_workflow_contract.py && rg -n "workflow.json|release=115|species=\\\"homo_sapiens\\\"|assembly=\\\"GRCh38\\\"|cache_flavor=\\\"vep\\\"|raise ValueError|raise FileNotFoundError" src/vepyr/_workflow_contract.py</automated>
  </verify>
  <done>The contract module can validate build and annotation requests and load the cache manifest without entering the Rust engine.</done>
</task>

<task type="auto">
  <name>Task 2: Wire build_cache() and annotate() to the preflight validators</name>
  <files>src/vepyr/__init__.py</files>
  <read_first>
    src/vepyr/__init__.py
    src/vepyr/_workflow_contract.py
    tests/test_supported_workflow_contract.py
    tests/test_annotate.py
  </read_first>
  <action>Update `src/vepyr/__init__.py` so `build_cache()` calls `validate_build_cache_request(release, species, assembly, method)` before any FTP path selection, directory creation, or local-cache work. Update `annotate()` so it calls `validate_annotation_request(cache_dir, reference_fasta=reference_fasta, everything=everything, hgvs=hgvs, merged=merged)` before creating the probe annotator. Keep the existing `reference_fasta is required when everything=True or hgvs=True` wording only if it becomes part of the new aggregated error text; otherwise replace it with a stricter message that also names the supported workflow values. Do not expose an override flag and do not allow `merged` or `refseq` through per D-02, D-04, and D-07.</action>
  <acceptance_criteria>
    `rg -n "validate_build_cache_request\\(|validate_annotation_request\\(" src/vepyr/__init__.py` returns two matches.
    The first `validate_build_cache_request(` call appears before `_download_cache(`.
    The first `validate_annotation_request(` call appears before `_create_annotator(`.
  </acceptance_criteria>
  <verify>
    <automated>python -m compileall src/vepyr/__init__.py && rg -n "validate_build_cache_request\\(|validate_annotation_request\\(|_download_cache\\(|_create_annotator\\(" src/vepyr/__init__.py</automated>
  </verify>
  <done>The public Python entrypoints reject unsupported workflow combinations before any network, cache conversion, or engine work begins.</done>
</task>

<task type="auto">
  <name>Task 3: Make the dedicated validation tests assert exact user-facing errors</name>
  <files>tests/test_supported_workflow_contract.py</files>
  <read_first>
    tests/test_supported_workflow_contract.py
    src/vepyr/_workflow_contract.py
    src/vepyr/__init__.py
    tests/test_annotate.py
  </read_first>
  <action>Update `tests/test_supported_workflow_contract.py` so each validation-path test asserts exact substrings for the supported tuple and failure cause. The required substrings are `homo_sapiens`, `GRCh38`, `115`, `vep`, `workflow.json`, and `reference_fasta` where applicable. Add one test that proves aggregated cache-manifest mismatches report more than one key in a single `ValueError`, for example by asserting both `release` and `assembly` appear in the same message.</action>
  <acceptance_criteria>
    `rg -n "homo_sapiens|GRCh38|115|vep|workflow.json|reference_fasta" tests/test_supported_workflow_contract.py` returns matches.
    The file contains one test whose `match=` or assertion string includes both `release` and `assembly`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_supported_workflow_contract.py -q -x</automated>
  </verify>
  <done>The new contract tests pass and prove that unsupported-scope failures are explicit, Python-side, and actionable.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_supported_workflow_contract.py -q -x`.
Run `uv run pytest tests/test_annotate.py -q -x` to confirm existing annotation entrypoint tests still pass with the stricter preflight behavior.
</verification>

<success_criteria>
Calling `build_cache()` or `annotate()` with unsupported release/species/assembly/cache-flavor/FASTA combinations now fails in Python with explicit supported-workflow errors before `_download_cache()` or `_create_annotator()`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-supported-workflow-contract/01-supported-workflow-contract-02-SUMMARY.md`
</output>
