---
phase: 01-supported-workflow-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/vepyr/_workflow_contract.py
  - src/vepyr/__init__.py
  - tests/data/golden/prepare.py
  - tests/test_supported_workflow_contract.py
  - tests/test_golden.py
  - README.md
autonomous: true
requirements:
  - CACHE-03
  - SCOPE-01
must_haves:
  truths:
    - "Prepared caches expose release, species, assembly, cache flavor, and reference contract metadata before annotation starts."
    - "Users can discover the supported v1 workflow from repository docs and runtime metadata without reading Phase 1 notes."
  artifacts:
    - path: "src/vepyr/_workflow_contract.py"
      provides: "Manifest builder with stable JSON field names."
      contains: "schema_version"
    - path: "src/vepyr/__init__.py"
      provides: "Manifest persistence during cache preparation and exported runtime metadata."
      contains: "SUPPORTED_WORKFLOW"
    - path: "README.md"
      provides: "User-facing documentation of the pinned workflow contract."
      contains: "homo_sapiens"
  key_links:
    - from: "src/vepyr/__init__.py"
      to: "workflow.json"
      via: "build_cache writes manifest next to parquet output"
      pattern: "workflow.json"
    - from: "tests/data/golden/prepare.py"
      to: "tests/data/golden/cache/workflow.json"
      via: "fixture preparation writes the same manifest schema for the trimmed cache"
      pattern: "workflow.json"
---

<objective>
Persist supported-workflow metadata in prepared caches and expose the same contract in runtime/docs artifacts.

Purpose: Satisfy cache discoverability and make later parity and benchmark phases reuse one unambiguous workflow definition.
Output: Stable cache manifest files, read-only runtime metadata, and repo documentation tied to the same supported tuple.
</objective>

<execution_context>
@/Users/mwiewior/research/git/vepyr/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mwiewior/research/git/vepyr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-supported-workflow-contract/01-CONTEXT.md
@.planning/phases/01-supported-workflow-contract/01-RESEARCH.md
@.planning/phases/01-supported-workflow-contract/01-VALIDATION.md
@.planning/phases/01-supported-workflow-contract/01-02-PLAN.md
@src/vepyr/_workflow_contract.py
@src/vepyr/__init__.py
@tests/data/golden/prepare.py
@tests/test_golden.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist a stable workflow manifest for built caches and fixtures</name>
  <files>src/vepyr/_workflow_contract.py, src/vepyr/__init__.py, tests/data/golden/prepare.py</files>
  <read_first>
    src/vepyr/_workflow_contract.py
    src/vepyr/__init__.py
    tests/data/golden/prepare.py
    tests/test_supported_workflow_contract.py
    tests/data/golden/cache
  </read_first>
  <action>Implement `build_cache_manifest()` in `src/vepyr/_workflow_contract.py` so it writes `<cache_dir>/workflow.json` with these exact top-level keys: `schema_version`, `species`, `assembly`, `release`, `cache_flavor`, and `reference_contract`. The values must encode the supported tuple `homo_sapiens`, `GRCh38`, `115`, and `vep`. The nested `reference_contract` object must contain `reference_name`, `fixture_source`, `contig_alias_style`, and `derived_fixture`. Update `build_cache()` in `src/vepyr/__init__.py` to call `build_cache_manifest(output_dir, derived_fixture=False)` after all parquet entities are written and also when `local_cache` is used for conversion. Update `tests/data/golden/prepare.py` to write the same `workflow.json` into `tests/data/golden/cache/` with `derived_fixture=True` after trimming the cache fixture from `Homo_sapiens.GRCh38.dna.primary_assembly.fa`.</action>
  <acceptance_criteria>
    `rg -n "\"schema_version\"|\"species\"|\"assembly\"|\"release\"|\"cache_flavor\"|\"reference_contract\"" src/vepyr/_workflow_contract.py` returns matches.
    `rg -n "build_cache_manifest\\(" src/vepyr/__init__.py tests/data/golden/prepare.py` returns matches.
    The manifest path is exactly `workflow.json`, not a hidden file or directory-name convention.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_supported_workflow_contract.py -q -x</automated>
  </verify>
  <done>Built caches and the golden cache fixture both produce a readable `workflow.json` manifest that exposes release/species/assembly/cache-flavor/reference metadata before annotation starts.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the supported workflow through runtime metadata and repository docs</name>
  <files>src/vepyr/__init__.py, README.md, tests/test_supported_workflow_contract.py, tests/test_golden.py</files>
  <read_first>
    src/vepyr/__init__.py
    README.md
    tests/test_supported_workflow_contract.py
    tests/test_golden.py
    .planning/phases/01-supported-workflow-contract/01-CONTEXT.md
  </read_first>
  <action>Export a read-only runtime metadata surface from `src/vepyr/__init__.py` by adding `SUPPORTED_WORKFLOW` to `__all__` and wiring it to the dict returned by `build_supported_workflow_dict()`. The exported dict must expose the keys `species`, `assembly`, `release`, `cache_flavor`, and `reference_contract`. Update `README.md` to document the exact supported v1 workflow as `homo_sapiens`, `GRCh38`, release `115`, standard Ensembl `vep` cache only, and one FASTA contract derived from `Homo_sapiens.GRCh38.dna.primary_assembly.fa`. Update `tests/test_supported_workflow_contract.py` to assert that `vepyr.SUPPORTED_WORKFLOW` exposes those keys and values. Update the header comment in `tests/test_golden.py` so the golden fixture text explicitly names release `115`, cache flavor `vep`, and the trimmed-primary-assembly FASTA derivation.</action>
  <acceptance_criteria>
    `rg -n "__all__ = \\[\"build_cache\", \"annotate\", \"SUPPORTED_WORKFLOW\"\\]" src/vepyr/__init__.py` returns a match.
    `rg -n "homo_sapiens|GRCh38|115|vep|Homo_sapiens.GRCh38.dna.primary_assembly.fa" README.md tests/test_golden.py tests/test_supported_workflow_contract.py` returns matches.
    `vepyr.SUPPORTED_WORKFLOW` is asserted in the test file with exact keys `species`, `assembly`, `release`, `cache_flavor`, and `reference_contract`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_supported_workflow_contract.py -q -x && uv run pytest tests/test_golden.py -q -x</automated>
  </verify>
  <done>The supported workflow is discoverable from cache metadata, a public read-only runtime constant, and repository docs/tests using the same exact values.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_supported_workflow_contract.py -q -x`.
Run `uv run pytest tests/test_golden.py -q -x`.
Optionally inspect one produced `workflow.json` manually to confirm the stored values match `homo_sapiens`, `GRCh38`, `115`, and `vep`.
</verification>

<success_criteria>
Prepared caches expose a stable `workflow.json` manifest, `vepyr.SUPPORTED_WORKFLOW` exposes the same tuple at runtime, and the repo documentation names the exact same supported contract without ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/01-supported-workflow-contract/01-supported-workflow-contract-03-SUMMARY.md`
</output>
